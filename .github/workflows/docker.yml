name: Docker
on:
  push:
    branches:
      - main
jobs:
  build:
    uses: cloud-cli/workflows/.github/workflows/docker-ghcr-build.yml@main
    with:
      name: cloud-cli/d0
      buildCommand: "pnpm i && pnpm build"